# ── Frontend (safe to expose; embedded in the built bundle) ──────────────────
# Point at the SAME Supabase project and PCO OAuth app as BFC Sunday Ops.
VITE_SUPABASE_URL=https://jrvootvytlzrymwoufzu.supabase.co
VITE_SUPABASE_ANON_KEY=<same anon key as Sunday Ops .env.local>
VITE_PCO_CLIENT_ID=<same PCO OAuth client id as Sunday Ops>

# ── Server-side (Supabase Edge Function secrets — set in the Supabase project,
#    NOT here, NOT committed). Listed for reference only. Most already exist on
#    the shared project because Sunday Ops set them.
# SUPABASE_SERVICE_KEY=...
# PCO_CLIENT_ID=...
# PCO_CLIENT_SECRET=...
# PCO_APP_ID=...            # Basic-auth app id for reading plan times
# PCO_SECRET=...            # Basic-auth secret
# MONDAY_API_TOKEN=...
# MONDAY_BOARD_ID=...       # Production Tasks board
# GOOGLE_SERVICE_ACCOUNT_CLIENT_EMAIL=...
# GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY=...
